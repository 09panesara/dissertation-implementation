from kMedians import kMedians
import numpy as np

def read_action_sequences(filepath):
    keypoints
    return keypoints

def get_action_sequences_for_emotion(action_sequences, emotion):
    return keypoints

def generate_lexicon(emotion):
    '''
    Generates lexicon for reference poseset per emotion category
    :return:
    '''

    # obtain action sequences for emotion
    action_sequences = read_action_sequences('data/')
    action_sequences = get_action_sequences_for_emotion(action_sequences, emotion)

    # Carry out k-medians
    kMedians = kMedians()





